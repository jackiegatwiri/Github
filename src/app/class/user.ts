export class User {
    constructor
        (public name:string, public company:string, public login:string, public avatar_url, public repository:number, public location:any, public followers:number, public following:number, public html_url, public created_at)
        {}
}
